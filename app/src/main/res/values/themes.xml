<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PulseCinemaAndroidApp" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:statusBarColor">@color/main_color</item>

        <!-- Navigation Bar Color (Bottom Bar) -->
        <item name="android:navigationBarColor">@color/main_color</item>

        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.PulseCinemaAndroidApp" parent="Base.Theme.PulseCinemaAndroidApp" />

</resources>